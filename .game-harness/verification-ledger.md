# Verification Ledger

Stage: M1 Protocol Proof
Last Updated: 2026-05-18

## Current Verdict

CONDITIONAL PASS: M1 technical proof now passes for backend checks, deterministic conversation suspicion fixtures, ordered same-NPC conversation turns, trajectory diversity verification, Godot checks, Codex gameplay QA probe, playable Evidence Pack export, shell/runtime/playable Evidence Pack schema validation, localization smoke, keyboard-look smoke, bridge fallback smoke, and renderer-backed visual capture.

This is a technical pass, not product closure. It proves the local protocol path and Evidence contract. It does not prove release readiness, live provider availability, external player comprehension, or public copy.

Small complete prologue/demo verdict: NOT VERIFIED. Current lane evidence internally proves local `Same Order` route contrast: clean cover, repair recovery, soft report, and inquest outcomes from the same Store Clerk prompt set. This Store/Station lane is now explicitly treated as the first small proof cell for environment-first NPC social simulation, not as the whole game premise. Fresh smoke/capture proves HUD typed input, delayed-answer Evidence, current record props, civic economy account-credit/trust/burden/attention values, latest ledger actor/action, fallback-only provider state, investigation trail subject/examiner/basis, exact Store ledger citation, live HUD record-chain copy, outcome consequence-chain copy, route-specific role-action recap, and readable renderer-backed screenshots/contact sheet. Codex gameplay QA also runs the same cell through the stable `PlayableSession.debug_codex_gameplay_action` API. Latest Godot 4.7-beta2 proof now covers PCK export, PCK `--main-pack` route smoke, macOS app zip export, packaged app launch, and packaged app route smoke with the same live HUD record-chain requirement. It still does not prove manual replay by a human player or external player comprehension.

Remaining blockers before advancing beyond M1:
- external player comprehension is still untested, with raw notes at `0 / 3`.
- QA/Producer product closure remains conditional until strict fresh-player
  review reaches pass-candidate status and accepted findings are copied into
  the external comprehension ledger.
- live backend/provider integration inside Godot remains future evidence, not
  an M1 fallback-only blocker. Do not claim live API-backed NPC proposals until
  runtime preflight proves live access and configured model availability.
- current manual readability review passes with Store/Station record-prop
  close-ups; external comprehension remains pending.

## M1 Technical Pass vs Product Blockers

| Area | Current Result | Release Truth |
|---|---|---|
| Local backend/Godot protocol checks | pass | May claim only local verified M1 technical proof. |
| Evidence Pack validation | pass | May claim schema-validated shell/runtime/playable Evidence Packs after checks pass. |
| Live API proposal provider | not proven | Must not claim live API-backed NPC proposals. M1 provider mode is fallback-only. |
| Provider action proposal boundary | contract pass | May claim provider-shaped proposals are constrained to available actions and same ledger outcomes; must not claim live provider dispatch. |
| Godot backend bridge fallback | pass | May claim controlled readiness fallback smoke, not live integration. |
| `gpt-5.4-mini` model access | not proven | Preferred only when runtime provider verification and budgeted live smoke confirm availability. |
| External player comprehension | pending | Same Order proxy dry run and product council review pass/conditional, but must not claim product validation or player-understood loop until fresh tester notes pass. |
| Exported build/setup path | PCK route proof plus packaged app export/launch/route proof | May claim current latest-Godot export, app launch, and packaged route proof. Must not claim public install readiness. |
| Canonical route event validation | internal pass | May claim only internal route-proof validation; public route-contrast proof still requires manual replay/readability and player comprehension. |

## Completion Truth Gates

| Gate | Current Result | Required Evidence Before Demo Completion |
|---|---|---|
| M1 technical proof | pass | Keep backend, Godot, Evidence, diversity, localization, keyboard, bridge, and renderer-backed visual checks passing. |
| Complete prologue route | internal pass | Godot smoke proves Store Clerk prompt -> route-specific choices -> deterministic clean cover, repair recovery, soft report, and inquest/session end. Human manual play is still unverified. |
| Safe/risky/recoverable outcome contrast | internal pass | `playable_slice_smoke.gd` runs fresh Same Order routes for clean cover, repair recovery, soft report, and inquest. Backend `validateGodotEvidencePackSameOrderRouteProofs` validates route proofs against embedded canonical route events. Manual replay and external comprehension remain pending. |
| Visual readability | current pass, external comprehension pending | Fresh renderer capture verifies through the Same Order visual evidence proxy. Manual readability review passes for HUD, typed input, provider state, latest ledger HUD line, Store/Station record-prop close-ups, repair correction-slip close-up, and inquest panel. Prop proof shots now isolate record objects by hiding non-record world dressing and the player body, but they remain state-review evidence, not standalone player-teaching proof. |
| Asset BOM | pass | Same Order asset BOM verifies local Kenney CC0 source packs, local license files, project-authored procedural record props, HUD files, and M1 audio scope. |
| UI readability | internal pass, external comprehension pending | Fresh smoke/capture proves prompt, three choices, typed input, delayed-answer warning copy, localized record summaries, investigation trail subject/examiner/basis, why-line, suspicion/report pressure, fallback-only provider state, deterministic end controls, latest ledger event, actor role, validated action, exact cited Store ledger ID, visible labeled record props, civic economy panel values including account credit, and outcome copy that links player speech/delay to Store record, queue reaction, Park notice, report handoff, Station citation, Studio review block, contact refusal, and Station Officer role action. |
| Player comprehension | proxy pass, packet ready, external pending | Proxy report proves the current Evidence Pack exposes the C1-C7 cause chain, including ordered validated-action trails through agentic route proofs, provider scheduling, and dispatch packets. The playtest packet defines the blind three-tester protocol, route cards, typed-input inquest operation, latest-ledger actor-role/validated-action check, observation checklist, and session-note template. External tester notes must still show players understand they are being investigated and can connect text to consequence. |
| Provider scheduling | contract pass, live pending | Backend provider scheduling defines 27 bounded role-agent jobs with action descriptors, allowed fields, forbidden authority fields, deterministic fallback wording, locked accepted actions, routine acceptance, public routine vouch, trust-gated local tip sharing, Studio PM review invitation from the same public vouch, Studio PM conditional review from a public repair notice, Studio PM review deferral from a public warning, Studio PM review block from the Station citation, repair acceptance, public repair notice, public rumor, soft-report service pause, queue exit after paused service, exact Station citation, and Waiting Customer contact refusal after citation. Live Godot dispatch is still unverified. |
| Provider dispatch packets | contract pass, live pending | Backend provider dispatch contract converts the scheduled jobs into `/v1/npc/decision` packets that pass schema and bounded behavior while carrying the same action descriptors into provider context. Live HTTP/Godot dispatch is still unverified. |
| Provider/export truth | pass for fallback-only M1 | Fallback-only M1 decision is recorded and current HUD/Evidence Pack/capture prove that mode. Latest PCK export smoke and packaged app route smoke preserve that mode. |
| Live authority integration | fallback-only for M1 | Current playable smoke declares `godot_local_conversation_runtime`; public/demo copy must treat this as fallback-only unless future live backend/runtime integration is proven. |

## Command Evidence

| Command | Result | Output Summary | Artifact |
|---|---|---|---|
| `node /Users/naem1023/git/game-studio/tools/check-project.mjs /Users/naem1023/git/dream-of-one` | pass | Required Game Studio directories/docs present. | `.game-studio/`, `docs/framework/` |
| `npm run check --prefix backend/npc-runtime` | pass | TypeScript build and 83 integration tests passed, including OpenAI provider model fallback, API conversation authority rejection, conversation suspicion fixtures, ordered conversation turns, playable Evidence Pack schema, Same Order inquest proof validation, canonical route-proof validation, and trajectory diversity tests. | `backend/npc-runtime/` |
| `npm run check --prefix backend/npc-runtime` | pass | TypeScript build and 131 integration tests passed after refreshing playable evidence for conditional review from the public repair notice. Coverage includes `post_repair_notice` changing `park_notice_board` to `repaired`, Studio PM `offer_conditional_review` changing `studio_review_queue` to `conditional`, Waiting Customer perceiving `park_notice_board` before `share_local_tip` and `keep_distance`, Waiting Customer `accept_routine`, `note_wary`, `accept_repair`, `leave_queue`, and `refuse_contact`; Park Witness `vouch_routine`, `post_warning`, `post_repair_notice`, and `post_rumor`; Store Manager `pause_service`; OpenAI provider boundaries; Same Order player comprehension packet; asset BOM; provider dispatch packets; provider scheduling; storylet runtime map; visual evidence proxy; comprehension proxy; provider action comparison; playable evidence comparison proof; available-action scoping; provider-off ledger preservation; and unsupported provider authority-field rejection. | `backend/npc-runtime/` |
| `PATH=/opt/homebrew/bin:$PATH npm run openai:proposal-smoke --prefix backend/npc-runtime` | pass, skipped live call | Printed `gpt-5.4-mini` config, fallback models, and `$0.01` estimated request budget; skipped because `OPENAI_PROPOSAL_LIVE_TEST` was not set. | `backend/npc-runtime/src/tools/openai-proposal-smoke.ts` |
| `PATH=/opt/homebrew/bin:$PATH OPENAI_PROPOSAL_LIVE_TEST=1 npm run openai:proposal-smoke --prefix backend/npc-runtime` | pass, skipped live call | Confirmed the live smoke refuses to treat Codex ChatGPT login as an API key; skipped because `OPENAI_API_KEY` is missing. Live provider access remains unproven. | `.game-harness/provider/openai-codex-auth-budget-2026-05-17.md` |
| `/opt/homebrew/bin/godot-latest --version` | pass | Godot 4.7-beta2 is installed as the repo's latest published Godot command. Homebrew `godot` remains stable 4.6.2. | `/opt/homebrew/bin/godot-latest`, `/Applications/Godot-4.7-beta2.app` |
| `/opt/homebrew/bin/godot-latest --headless --import --path godot` | pass | Fresh latest-Godot import completed after enabling ETC2/ASTC texture import for macOS universal export. | `godot/project.godot` |
| `GODOT_PATH=/opt/homebrew/bin/godot-latest bash "$HOME/.agents/skills/godot-best-practice/scripts/check_gd_syntax.sh" godot` | pass | 22 GDScript files checked with Godot 4.7-beta2, 0 errors after adding conditional Studio review state, HUD repair-chain copy, and probe checks. | `godot/**/*.gd` |
| `/opt/homebrew/bin/godot-latest --headless --path godot --script res://tools/scene_load_smoke.gd` | pass | Main scene loaded with player, HUD, playable session, 4 landmarks, 6 NPCs including `NPC_Studio_PM` and `NPC_Waiting_Customer`, 4 text panels, 4 interaction zones, 3 routes, and 191 visual asset/cue instances. | `godot/scenes/main.tscn` |
| `/opt/homebrew/bin/godot-latest --headless --path godot --script res://tools/evidence_run.gd` | pass | Shell Evidence Pack exported with latest Godot and 6 actor signatures including `NPC_Waiting_Customer`. | `data/evidence/godot/shell/dre_171_shell_evidence.json` |
| `/opt/homebrew/bin/godot-latest --headless --path godot --script res://tools/runtime_slice_smoke.gd` | pass | Runtime slice exported command validation/rejection, fallback, and Station intake Evidence with latest Godot. | `data/evidence/godot/runtime-slice/dre_171_runtime_slice_evidence.json` |
| `/opt/homebrew/bin/godot-latest --headless --path godot --script res://tools/playable_slice_smoke.gd` | pass | Latest Godot 4.7-beta2 playable smoke preserved Same Order route proofs, `socialObservationTrace`, typed input, response-hesitation Evidence, fallback-only provider state, civic economy panel values, HUD investigation trail, HUD-visible social reaction, and terminal outcome bodies with route-specific role-action/social-reaction recap. Clean cover proves public vouch, local tip, and Studio review invitation; repair now proves Waiting Customer `accept_repair`, visible `NPC_Waiting_Customer` state `repair_accepted` with `수습 수락`, Park Witness `post_repair_notice`, visible `NPC_Park_Witness` state `repaired` with `수습 게시`, `park_notice_board=repaired`, Studio PM `offer_conditional_review`, `studio_review_queue=conditional`, visible `NPC_Studio_PM` state `conditional` with `조건부 리뷰`, and HUD repair chain `기억 공백 발화 -> 정정표 -> 대기줄 수습 -> 공개 수습 게시 -> 조건부 리뷰`; suspicious cover proves public warning, distance, and Studio review deferral; soft report proves public rumor, service pause, queue exit, and HUD chain `플레이어 발화 -> 상점 기록 -> 공원 게시 -> 응대 중단 -> 줄 이탈 -> 보고 접수`; inquest proves Park rumor, Store Manager handoff, Station citation, Studio review block, and Waiting Customer contact refusal. | `data/evidence/godot/playable-slice/dre_171_playable_slice_evidence.json` |
| `/opt/homebrew/bin/godot-latest --headless --path godot --script res://tools/codex_gameplay_probe.gd` | pass | Codex gameplay QA probe drove the running Same Order scene through `PlayableSession.debug_codex_gameplay_action`, reached `inquest`, and wrote JSON/Markdown QA artifacts. The report passes with `acceptedSteps=15`, `explainability=14/14`, `routeReportPassCount=5 / 5`; it now focuses and inspects `civic_economy_panel` as a player-readable social-pressure record with current `잔액`, `신뢰`, `부담`, `주목`, and recent ledger `변화` deltas. It also inspects world record props with current affordance state: `park_notice_board=rumored` shows currently open actions, while `studio_review_queue=blocked` says `현재 열린 행동: 없음` even though its role/action map remains readable. NPC reaction inspection still shows `가능 조건` and `값 변화`, proving the player can read the object state, cited ledger requirement, and toy economy effect that made `리뷰 차단` and `접촉 거부` valid. This is Codex-run QA, not external player comprehension evidence. | `data/evidence/godot/codex-gameplay-probe/dre_171_codex_gameplay_probe.json`, `data/evidence/godot/codex-gameplay-probe/dre_171_codex_gameplay_probe.md` |
| `.game-harness/scripts/run-same-order-comprehension-session.sh --codex-probe-status` | pass | The facilitator helper can print packaged-app preflight plus the probe path. Current Codex gameplay artifacts have JSON SHA-256 `e2da3f0766d028d9097e63e30db973283af33ada1f3781ccf21899c180cea5b6`, Markdown SHA-256 `b47549434397cd9dae7ab330f1d0cd03494e00be27384c7e3231fabbec0b61ab`, `stage=inquest`, 13 accepted Codex/player actions, `aiPlayerReport=true`, `explainability=13/13`, route reports `5/5`, and `humanEvidence=false`. This connects Codex-run QA to the human comprehension workflow without counting it as player evidence. | `.game-harness/scripts/run-same-order-comprehension-session.sh`, `data/evidence/godot/codex-gameplay-probe/dre_171_codex_gameplay_probe.json`, `data/evidence/godot/codex-gameplay-probe/dre_171_codex_gameplay_probe.md` |
| `/opt/homebrew/bin/godot-latest --quit-after 2400 --path godot --script res://tools/visual_capture.gd` | pass | Latest Godot 4.7-beta2 renderer capture produced the current screenshots/contact sheet with `blockedChecks: []`, current civic economy panel label `잔액 3 | 신뢰 0 | 부담 93 | 주목 70`, civic ledger `8건`, Studio review queue `blocked`, live HUD consequence copy linking speech/delay -> Store record -> queue reaction -> Park notice -> report handoff -> Station citation -> Studio review block -> contact refusal, and inquest outcome text linking the same chain through final inquest. | `data/evidence/godot/visual-capture/manifest.json` |
| `PATH=/opt/homebrew/bin:$PATH npm run same-order:attach-playability-reports --prefix backend/npc-runtime` | pass | Reattached backend-generated agentic, provider, asset, visual, comprehension, and playtest reports to the fresh Godot Evidence Pack. | `backend/npc-runtime/src/tools/attach-same-order-playability-reports.ts` |
| `/opt/homebrew/bin/godot-latest --headless --path godot --export-pack "macOS PCK" /private/tmp/dream-of-one-export-proof-4.7/dream-of-one-same-order.pck` | pass | Created a 5.1M latest-Godot PCK artifact after repair recovery began writing `park_notice_board=repaired` as a public environment record; PCK SHA-256 `35d44fade6ea9a6645c3323fafdbae072dec0a84a09c1fea20304b4567a54294`. | `/private/tmp/dream-of-one-export-proof-4.7/dream-of-one-same-order.pck`, `godot/export_presets.cfg` |
| `DREAM_OF_ONE_PLAYABLE_EVIDENCE_OUTPUT=/private/tmp/dream-of-one-export-proof-4.7/main-pack-playable-evidence.json /opt/homebrew/bin/godot-latest --headless --log-file /private/tmp/dream-of-one-export-proof-4.7/main-pack-smoke.log --main-pack /private/tmp/dream-of-one-export-proof-4.7/dream-of-one-same-order.pck --script res://tools/playable_slice_smoke.gd` | pass | Loaded the refreshed latest-Godot exported PCK through `--main-pack` and preserved Same Order typed input, response-hesitation Evidence, fallback-only provider mode with `liveVerified = false`, civic economy account-credit/trust/burden/attention panel values, latest route proofs for clean cover, repair recovery, soft report, and inquest, including repair public notice `park_notice_board=repaired` and HUD repair chain `기억 공백 발화 -> 정정표 -> 대기줄 수습 -> 공개 수습 게시`. Main-pack evidence SHA-256 `1474022966c7b7020d7cdf4d2e3a1ebe383baf3745b79ff23b9cfe519bae116f`. | `/private/tmp/dream-of-one-export-proof-4.7/main-pack-playable-evidence.json` |
| `/opt/homebrew/bin/godot-latest --headless --path godot --export-debug "macOS PCK" /private/tmp/dream-of-one-export-proof-4.7/dream-of-one-same-order-macos.zip` | pass | Exported a macOS debug app zip with Godot 4.7-beta2 templates after repair public notice became environment state; ZIP SHA-256 `11bbc6430c4f58258c4ac438f612c82cfa8d6b6a9f6071a4e66be72928a43f01`. | `/private/tmp/dream-of-one-export-proof-4.7/dream-of-one-same-order-macos.zip` |
| `/private/tmp/dream-of-one-export-proof-4.7/app/Dream\ of\ One\ Godot\ Shell.app/Contents/MacOS/Dream\ of\ One\ Godot\ Shell --headless --log-file /private/tmp/dream-of-one-export-proof-4.7/app-launch.log --quit-after 3` | pass | Packaged app starts and exits cleanly in headless mode. This is launch proof only. | `/private/tmp/dream-of-one-export-proof-4.7/app-launch.log` |
| `DREAM_OF_ONE_PACKAGED_ROUTE_EVIDENCE_OUTPUT=/private/tmp/dream-of-one-export-proof-4.7/app-route-evidence.json /private/tmp/dream-of-one-export-proof-4.7/app/Dream\ of\ One\ Godot\ Shell.app/Contents/MacOS/Dream\ of\ One\ Godot\ Shell --headless --log-file /private/tmp/dream-of-one-export-proof-4.7/app-route-smoke.log` | pass | Refreshed packaged app binary reaches Same Order inquest, preserves response-hesitation Evidence, typed free input, fallback-only provider mode with `liveVerified = false`, civic economy panel label `잔액 3 | 신뢰 0 | 부담 93 | 주목 70`, exact Station citation `civic-ledger-6 -> civic-ledger-5`, Studio review block `civic-ledger-7 -> civic-ledger-6`, latest contact refusal `civic-ledger-8 -> civic-ledger-6`, and writes `playability.packagedRouteSmokeProof` with HUD examiner/subject checks, live HUD record-chain proof, outcome-chain proof, and civic economy checks. Preflight confirms route evidence mtime is newer than the packaged app binary. App-route evidence SHA-256 `f86316825bc6c00a89eff13bcf9f4b34dd52ae2c70aa6ecd8c2be714fae3370d`. | `/private/tmp/dream-of-one-export-proof-4.7/app-route-evidence.json`, `godot/scripts/runtime/packaged_route_smoke.gd` |
| `jq '{pass: .playability.packagedRouteSmokeProof.pass, liveRecordChain: .playability.packagedRouteSmokeProof.outcomeChecks.liveRecordChain, consequence: .playability.packagedRouteSmokeProof.consequenceLabel}' /private/tmp/dream-of-one-export-proof-4.7/app-route-evidence.json` | pass | Packaged route evidence reports `liveRecordChain: true` and preserves the exported HUD consequence label through `흐름: 플레이어 발화/응답 지연 -> 상점 기록 -> 대기줄 반응 -> 공원 게시 -> 보고 전달 -> 스테이션 인용 -> 스튜디오 리뷰 차단 -> 접촉 거부`. | `/private/tmp/dream-of-one-export-proof-4.7/app-route-evidence.json` |
| `node --import ./backend/npc-runtime/node_modules/tsx/dist/loader.mjs -e "<validate app-route-evidence>"` | pass | Packaged app route Evidence validates through backend `validateGodotEvidencePack` and `validateGodotEvidencePackConversationSuspicionProof`; `playability.packagedRouteSmokeProof.pass` is true and preserves exported HUD trail plus `outcomeChecks.liveRecordChain`, `outcomeChecks.speechDelayRecordChain`, and `outcomeChecks.stationOfficerRoleAction`. | `/private/tmp/dream-of-one-export-proof-4.7/app-route-evidence.json` |
| `bash -n .game-harness/scripts/run-same-order-comprehension-session.sh` | pass | Facilitator helper parses. It validates packaged evidence, can refresh/print Codex gameplay QA probe status, asks for tester label/language/fresh status before launch, refuses to launch for non-fresh or unmarked participants, suppresses successful packaged-preflight detail on the default live path, then launches the packaged app with `open -W` after a neutral pre-play line and a spoiler-safe private-terminal reminder. After the app closes, it reminds the facilitator to ask what happened, what changed after speech/typed input, and capture the tester's own wording before explaining the design. It writes raw session notes only after an actual tester run, including fresh-tester status, exact build path, packaged route evidence path, app/evidence SHA-256, Codex probe path/SHA/status, Codex gameplay QA route reports, preflight result, fallback-only provider state, packaged outcome-chain proof, packaged civic economy proof, route coverage, safe/risky observation, civic economy detail noticed, dialogue-to-record-to-consequence-to-role-action explanation, and direct quote fields; it is not player comprehension evidence by itself. | `.game-harness/scripts/run-same-order-comprehension-session.sh` |
| `.game-harness/scripts/run-same-order-comprehension-session.sh --preflight` | pass | Preflight checks the packaged app executable and packaged route evidence without launching the app. It fails if route evidence is older than the packaged app binary, and requires fallback-only provider mode, response-hesitation Evidence, typed free input, inquest outcome, exact Station citation `civic-ledger-6 -> civic-ledger-5`, Studio review block `civic-ledger-7 -> civic-ledger-6`, latest contact refusal `civic-ledger-8 -> civic-ledger-6`, packaged HUD examiner/subject proof, packaged live HUD record-chain proof, packaged outcome-chain proof, and packaged civic economy proof. | `.game-harness/scripts/run-same-order-comprehension-session.sh`, `/private/tmp/dream-of-one-export-proof-4.7/app-route-evidence.json` |
| `.game-harness/scripts/run-same-order-comprehension-session.sh --facilitator-pack` and `--worksheet` | pass | Facilitator pack and worksheet now include Codex gameplay QA probe path, SHA-256, status, route report summary for clean cover, repair recovery, suspicious cover, soft report, and inquest, and the boundary that it is setup proof only. The worksheet still requires observed fresh-player notes and direct quotes before it can become evidence. | `.game-harness/scripts/run-same-order-comprehension-session.sh`, `.game-harness/comprehension/manual-sessions/README.md` |
| `.game-harness/scripts/run-same-order-comprehension-session.sh --session-kit-output <dir>` | pass | Generated facilitator-only session kit writes `facilitator-pack.md`, `tester-invite.md`, `recruitment-facilitator-card.md`, `facilitator-card.md`, `after-play-debrief.md`, `raw-session-worksheet.md`, `gate-status.txt`, `README.md`, and `session-kit-manifest.json`. It fails if Codex gameplay QA status is not pass or if the Codex probe JSON/Markdown are older than watched Godot proof-cell files, copies facilitator-only snapshots `codex-gameplay-probe.json` and `codex-gameplay-report.md` into the kit, includes route reports `clean_cover`, `repair_recovered`, `cover_held_under_suspicion`, `soft_report`, and `inquest_opened` in README and manifest, then runs `--verify-session-kit` against its own output and saves `session-kit-self-check.txt`. Verification now also checks the copied Markdown report for action path, player-readable cause chain, route outcomes, final visible state, role actions, NPC-to-NPC observations, and the external-comprehension boundary. The manifest marks `humanEvidence=false` and `closesGoal=false`, so an AI tool can inspect setup binding without confusing it for player comprehension. | `.game-harness/scripts/run-same-order-comprehension-session.sh` |
| `.game-harness/scripts/run-same-order-comprehension-session.sh --verify-session-kit <dir>` | pass | Verifies an existing session kit has all required files, manifest schema, `testerFacingFiles=["tester-invite.md"]`, fallback-only packaged proof flags, copied Codex QA JSON/Markdown hashes, copied probe schema/API/report flags, Codex source freshness at generation, and current freshness of the kit's copied Codex JSON/Markdown against watched Godot proof-cell files. It also verifies required Codex action catalog entries including `dialogue.choice.by_id` and `dialogue.choice.by_index`, an accepted `player.type.free_input` step with a non-empty payload that reaches inquest, copied player-visible HUD state, record props, civic ledger citation, Store Manager forwarding, Station Officer citation, manifest action/route data matching the copied Codex gameplay probe JSON, 5/5 passing Codex route reports, readable copied Markdown gameplay report content covering action path, cause chain, route outcomes, final visible state, role actions, NPC observations, and product boundary, README live-session handoff markers, `humanEvidence=false`, `closesGoal=false`, no spoiler terms in the tester invite, broader Station/record/risk/examined-role term leaks rejected with case-insensitive English matching, and neutral facilitator pre-play wording with stale "Station intake path" prompts rejected. Its success output now prints `Codex source freshness: pass...`, `Codex kit current source freshness: pass...`, and `README live-session handoff: pass`. This is setup verification only and cannot close the comprehension gate. | `.game-harness/scripts/run-same-order-comprehension-session.sh` |
| `bash -n .game-harness/scripts/review-same-order-comprehension-notes.sh` | pass | Raw-note review helper parses. It summarizes manual session files, returns `PENDING_TESTER_NOTES` when no actual tester notes exist, rejects weak or build-unbound notes, verifies referenced app/evidence paths and SHA-256, packaged HUD examiner/subject proof, packaged live HUD record-chain proof, packaged outcome-chain proof, packaged civic economy proof, and packaged evidence semantics, rejects checkbox placeholders as direct quotes, tracks civic economy detail as a separate comprehension count, requires explicit `Fresh tester: yes`, and only emits pass-candidate status when all three sessions are marked fresh with distinct tester labels and strict sample, build/preflight provenance, route, chain, quote, O1-O7, delayed-answer, and no-role-inversion thresholds are met. | `.game-harness/scripts/review-same-order-comprehension-notes.sh` |
| `.game-harness/scripts/review-same-order-comprehension-notes.sh --strict` | expected fail while pending | Strict mode returned `PENDING_TESTER_NOTES` with 0 sessions and exit code 1. This is the correct gate behavior until raw notes reach `PASS_CANDIDATE_REQUIRES_QUOTE_REVIEW`, so pending or weak notes cannot accidentally pass. | `.game-harness/scripts/review-same-order-comprehension-notes.sh` |
| `.game-harness/scripts/review-same-order-comprehension-notes.sh --ledger-draft <path>` | pass | Ledger draft mode writes a copyable human-review draft with counts, build/evidence provenance, route coverage, direct quotes, and O1-O7 fields. The draft explicitly does not close the external comprehension gate. | `.game-harness/scripts/review-same-order-comprehension-notes.sh` |
| AI-play goal lock | pass | The active goal prompt, loop state, and repo agent instructions now define Codex gameplay QA as a per-increment acceptance condition: Codex must be able to launch/probe the proof cell, list callable actions, act, inspect player-visible HUD/world/ledger/NPC state, and produce a readable player-perspective artifact before new player-facing scope is considered ready. This is the preferred fast check for player-view questions in an AI-built game; tests stay lean and protect deterministic contracts instead of replacing actual play. This remains internal development proof and does not replace external comprehension notes. | `.game-harness/active-goal-prompt.md`, `.game-harness/goal-loop-state.md`, `AGENTS.md` |
| Game Studio project-state alignment | pass | Project state no longer lists stale blockers for preset recorded-statement proof, pending manual typed input, unresolved provider/export decisions, or broad missing role review. It now records that typed input is proven through Godot/HUD/Codex QA, fallback-only provider mode and export setup are current, council/product review is conditional, and external fresh-player comprehension remains the active product gate. | `.game-studio/project-state.md`, `.game-harness/goal-loop-state.md` |
| GI-01 typed recorded speech closure | pass | Task and project-state routing no longer list manual recorded-statement UI or a free-input cut decision as open. Current evidence proves HUD typed free-input through Godot/HUD/packaged/Codex QA, and the legacy recorded-statement fallback remains internal only, so the next product action is fresh-player comprehension rather than reopening input wiring. | `.game-harness/tasks.md`, `.game-studio/project-state.md`, `.game-harness/continue-here.md`, `.game-harness/goal-loop-state.md` |
| Game Studio priority order alignment | pass | Project-state priority order now puts packaged fresh-player sessions, strict raw-note review, and M1/M2 product decision before issue splitting or any next Store/Station increment, keeping the repo aligned with the active hard gate. | `.game-studio/project-state.md`, `.game-harness/goal-loop-state.md` |
| Harness task ordering alignment | pass | Active tasks now defer four-week/agentic prototype issue splitting and all post-comprehension Store/Station issue creation until fresh-player comprehension evidence and the M1/M2 decision are recorded. The only active open proof task in that lane is running the simulator-adoption fresh-player sessions. | `.game-harness/tasks.md`, `.game-harness/goal-loop-state.md` |
| Comprehension status next-action clarity | pass | `--status` now puts the live observed-session helper first as the minimal path when a fresh tester is present, and treats no-spoiler recruitment plus session-kit creation/verification as optional facilitator setup. It also keeps the boundary that only observed fresh-player raw notes can move the gate. | `.game-harness/scripts/run-same-order-comprehension-session.sh`, `.game-harness/goal-loop-state.md` |
| Default live note prompt order | pass | The live observed-session helper now prints the post-play reminder, then asks for the tester's own explanation, perceived change, record detail, actor detail, chain explanation, delayed-answer detail, direct quotes, and role inversion before route labels, safe/risky classification, intervention, or pass/fail scoring. This keeps the raw note closer to the player's first read of the game. | `.game-harness/scripts/run-same-order-comprehension-session.sh`, `.game-harness/goal-loop-state.md` |
| Manual-session runbook simplification | pass | The manual-session README now starts with the one-command live helper for each fresh tester, keeps `--status` as a quick readiness check, moves recruitment/session-kit/worksheet/debrief/Codex detail modes to optional setup, and the live helper prints the raw note count, minimum remaining sessions, progress review command, and status command after writing a raw note. Once raw note count reaches three, it prints strict review and ledger-draft commands directly. This reduces facilitator friction without adding another internal gate. | `.game-harness/comprehension/manual-sessions/README.md`, `.game-harness/scripts/run-same-order-comprehension-session.sh`, `.game-harness/goal-loop-state.md` |
| Session kit live-run handoff clarity | pass | Generated session kit README files now name the kit re-verification command, the live observed-session helper command, the neutral-pre-play reminder, and the tester-facing boundary. The session kit verifier fails if those README handoff lines disappear and now prints `README live-session handoff: pass` on success, making the transition from AI/setup proof to human observation explicit without counting the kit as comprehension evidence. | `.game-harness/scripts/run-same-order-comprehension-session.sh`, `.game-harness/scripts/verify-comprehension-review-guards.sh`, `.game-harness/goal-loop-state.md` |
| Resume handoff alignment | pass | Continuation notes now open with the current 2026-05-18 blocker truth, direct the next worker to `--recruitment`, `--session-kit-output <dir>`, and `--verify-session-kit <dir>`, and warn that session kits are setup proof only. It also removes stale manual typed-input warning language in favor of the current Godot/HUD/packaged/Codex QA proof state. | `.game-harness/continue-here.md`, `.game-harness/goal-loop-state.md` |
| External comprehension pre-play wording alignment | pass | External comprehension run steps now use the neutral first-run line `Play this short scene without prior explanation until it stops or until 5 minutes pass.` and tell facilitators to verify generated session kits with `--verify-session-kit <dir>`. The comprehension guard now fails if the stale `Station intake path` pre-play hint returns to the external notes ledger. | `.game-harness/comprehension/same-order-external-comprehension-notes-2026-05-17.md`, `.game-harness/comprehension/manual-sessions/README.md`, `.game-harness/scripts/verify-comprehension-review-guards.sh` |
| M1 product gate council review | conditional pass | Director/Narrative/Systems/Godot roles pass; QA/Producer remain conditional because external tester notes are missing. Provider boundary is accepted for fallback-only M1. | `.game-harness/council/m1-product-gate-council-review-2026-05-17.md` |
| `/opt/homebrew/bin/godot-latest --headless --path godot --script res://tools/localization_smoke.gd` | pass | Default locale `ko`, switched locale `en`. | localization runtime |
| `/opt/homebrew/bin/godot-latest --headless --path godot --script res://tools/live_backend_bridge_smoke.gd` | pass | Mock-ready, missing-key fallback, and live-unavailable fallback paths preserve deterministic state authority. | `godot/tools/live_backend_bridge_smoke.gd` |
| `node --import tsx ... validateGodotEvidencePack(...)` | pass | Shell, runtime, and playable Evidence Packs validate through backend tests; playable pack preserves conversation identity, selected line, free-input hash, suspicion signals, deltas, and why-line. | `data/evidence/godot/**` |
| `/opt/homebrew/bin/godot-latest --headless --path godot --script res://tools/keyboard_look_smoke.gd` | pass | Keyboard look changes yaw/pitch without movement-driven auto-yaw. | `godot/tools/keyboard_look_smoke.gd` |

## Proof Validators

| Validator | Current Truth |
|---|---|
| `backend/npc-runtime/test/integration/conversation-suspicion.integration.test.ts` | Safe, risky, and free-input conversation classifications are deterministic backend behavior. |
| `backend/npc-runtime/test/integration/decision-service.integration.test.ts` | Same-NPC conversation turns with `conversation.turnId` stay ordered and are not latest-wins coalesced. |
| `backend/npc-runtime/test/integration/godot-runtime-schema.integration.test.ts` | Conversation Evidence requires identity fields, deterministic signal trace, selected line or free-input hash, deltas, and why-line. |
| `validateGodotEvidencePackConversationSuspicionProof` | Playable Evidence Pack must include the ordered Same Order conversation chain with one `conversationId`, turn continuity, final `inquest` stage, and report threshold proof. |
| `validateGodotEvidencePackSameOrderRouteProofs` | Playable Evidence Pack must include route proofs for `clean_cover`, `repair_recovered`, `soft_report`, and `inquest_opened`, with embedded canonical route events, thresholds, signals, final state, and required/forbidden events matching each outcome. |
| `backend/npc-runtime/test/integration/agentic-environment.integration.test.ts` | Accepted backend civic ledger events preserve the affordance that created them, so each record can be traced back to a role-authorized environment action. |
| `backend/npc-runtime/test/integration/agentic-environment.integration.test.ts` | Same Order environment actions are scoped by perception, object state, ledger knowledge, ledger kind, and simple economy thresholds. The current runtime validates Park Witness `vouch_routine` after Waiting Customer `accept_routine`, Waiting Customer `share_local_tip` after that public vouch when `localTrust >= 55`, Studio PM `invite_review` after the same public vouch, Park Witness `post_warning` after Waiting Customer `note_wary`, Waiting Customer `keep_distance` after public warning when `localTrust <= 45`, Store Manager `pause_service`, Waiting Customer `leave_queue` after paused service, and Waiting Customer `refuse_contact` after Station Officer `cite_record`. |
| `godot/tools/playable_slice_smoke.gd` | Fresh run proves route contrast, HUD typed input, response-hesitation Evidence, investigation trail subject/examiner/basis, latest ledger actor/action, selected action descriptors, fallback-only provider state, exact cited Store ledger ID, and route-specific terminal outcome copy with role-action recap. Current suspicious-cover ledger order is marked receipt -> wary queue note -> public warning -> distance kept -> Studio review deferred; current soft-report order is marked receipt -> clerk note -> queue delay -> public rumor -> manager note -> service pause -> queue left; current inquest ledger order is receipt mark -> clerk note -> queue delay -> public rumor -> manager forward -> Station citation -> Studio review block -> contact refusal. |
| `godot/tools/codex_gameplay_probe.gd` | Codex can run the active Godot proof cell through the stable `PlayableSession.debug_codex_gameplay_action` and `debug_codex_gameplay_snapshot` API, execute bounded player actions, inspect HUD/environment/ledger/NPC role-action state, and emit readable QA artifacts before human playtest. The JSON artifact includes `aiPlayerReport`, 5/5 route reports, clean-cover `accept_routine`, Park Witness `vouch_routine`, Waiting Customer `share_local_tip`, suspicious-cover Studio PM `defer_review`, soft-report `pause_service` and `leave_queue`, inquest Studio PM `block_review`, Waiting Customer `refuse_contact`, exact Station citation, latest ledger `queue_contact_refused`, inspected Studio PM basis fields tying `civic-ledger-7` to cited `civic-ledger-6`, and inspected Waiting Customer basis fields tying `civic-ledger-8` to cited `civic-ledger-6`. The Markdown sidecar summarizes the action path, route outcomes, final player-visible state, cause chain, role-action explanation, social observation explanation, and human-tester boundary. |
| `godot/tools/visual_capture.gd` | Fresh renderer capture proves world/HUD ledger visibility including actor role/action, HUD investigation trail, HUD typed input, fallback-only provider state, Station outcome citing the Store ledger entry, and outcome copy that names the Station Officer role action. |
| `backend/npc-runtime/test/integration/same-order-provider-action-comparison.integration.test.ts` | Provider-shaped Same Order proposals must choose from available environment actions, preserve provider-off ledger/object/economy outcomes and affordance provenance, and reject unsupported authority/state fields. |
| `backend/npc-runtime/test/integration/same-order-provider-scheduling.integration.test.ts` | Same Order provider scheduling must produce bounded role-agent jobs, deterministic fallback wording, action descriptor context, recent ledger affordance context, exact Station citation, and a playable Evidence Pack `providerSchedulingPlan` while keeping live Godot dispatch explicitly unverified. |
| `backend/npc-runtime/test/integration/same-order-provider-dispatch-contract.integration.test.ts` | Same Order provider dispatch must turn scheduled jobs into `/v1/npc/decision` packets, keep conversation authority fields rejected, carry action descriptors and recent ledger affordance context, pass bounded behavior, and carry `providerDispatchContract` while keeping live HTTP dispatch explicitly unverified. |
| `backend/npc-runtime/test/integration/same-order-storylet-runtime-map.integration.test.ts` | Same Order storylet runtime map binds each storylet beat to route ids, prompt/choice ids, required environment objects, validated runtime action step ids, scheduled provider job ids, ledger event kinds, affordances, and Evidence events. The playable Evidence Pack now carries `playability.storyletRuntimeMap`; this is internal alignment proof, not player comprehension. |
| `backend/npc-runtime/test/integration/same-order-comprehension-proxy.integration.test.ts` | Current playable Evidence Pack passes C1-C7 proxy checks while keeping `externalBlockerClosed: false`; C6 verifies ordered validated-action trails and removing the risky Station route fails the proxy. |
| `backend/npc-runtime/test/integration/same-order-player-comprehension-playtest.integration.test.ts` | Current playable Evidence Pack carries one blind three-tester comprehension packet with route contrast, C1-C7 questions, latest-ledger actor-role/validated-action checks, and `externalBlockerClosed: false`. |
| `backend/npc-runtime/test/integration/same-order-visual-evidence-proxy.integration.test.ts` | Fresh Store conversation screenshots and contact sheet have expected files, PNG signatures, dimensions, required capture roles, human-readability flags, typed input evidence, latest ledger actor/action evidence, and fallback-only provider state. Missing screenshot fixtures fail the proxy. |
| `backend/npc-runtime/test/integration/same-order-asset-bill-of-materials.integration.test.ts` | Same Order asset BOM must verify local source-pack licenses, referenced asset files, procedural Store/Station props in `worldRecordProps`, project-authored HUD files, M1 audio scope, and playable Evidence Pack `assetBillOfMaterials`. |
| `godot/tools/visual_capture.gd` | Store conversation capture expectations are aligned. Renderer-backed capture produced current PNG/contact-sheet proof with three-choice conversation panel, typed input, investigation trail, fallback-only provider state, latest ledger HUD line, and Store Clerk reaction cue. |
| `godot/scripts/runtime/packaged_route_smoke.gd` | Packaged app route smoke drives the inquest path inside the exported app, validates both runtime Evidence and HUD wording, validates outcome consequence-chain copy, and writes `playability.packagedRouteSmokeProof` for preflight to inspect. |

## Documentation Evidence

| Check | Result | Output Summary |
|---|---|---|
| Game Studio placeholder scan | pass | No TODO/TBD/PLACEHOLDER/FIXME in `docs/direction`, `docs/framework`, or `.game-studio`. |
| Small-loop economy research | pass | Web-backed economy research has been expanded with source/tap, pool, sink/drain, converter, gate, obligation, live-economy measurement, and low-budget operation-sim examples. Active direction/framework docs now require one economy hypothesis, one visible role-action change, one proof run, and no broad pressure, pricing, inventory, wages, rent, taxes, or shop-management expansion until the current social-record loop is readable. |
| release-truth search | conditional | Active direction docs use API provider premise. Older scenario/pitch/release docs with Codex CLI language are marked superseded/historical where touched. |

## Visual Evidence

| Artifact | Result | Output Summary |
|---|---|---|
| `/opt/homebrew/bin/godot-latest --headless --path godot --script res://tools/visual_capture.gd` | expected blocked | State expectations are aligned to Store conversation captures, but the headless display server cannot provide viewport pixels. Use the renderer-backed command below for PNG proof. | `data/evidence/godot/visual-capture/manifest.json` |
| `/opt/homebrew/bin/godot-latest --quit-after 2400 --path godot --script res://tools/visual_capture.gd` | pass | Produced eight nonblank Store conversation screenshots and a 1280x1440 contact sheet with no blocked checks. The current capture shows typed input, investigation trail, fallback-only provider state, latest ledger HUD line, latest NPC social reaction, pressure, why-line, Store/Station record-prop close-ups, civic economy panel account credit/trust/burden/attention values, repair-route attached correction slip, and inquest citation. The record-prop proof shots now use prop-focused review cameras and hide NPC/text clutter, non-record world dressing, and the player body for those shots only. | `data/evidence/godot/screenshots/01-opening-store-framing.png`, `data/evidence/godot/screenshots/08-repair-correction-slip-closeup.png`, `data/evidence/godot/visual-capture/contact-sheet.png` |
| Same Order visual evidence proxy | pass, fresh capture | Current capture files match manifest dimensions and required roles, including the repair-route correction slip close-up. The proxy marks `freshCapture: true`, typed input evidence fresh, internal recorded-statement fallback absent from the current typed-input proof, latest ledger actor/action evidence fresh, social-reaction expectations fresh, and required Store/Station record props visible/labeled in the manifest. Manual readability review passes with isolated prop close-ups while keeping HUD/inquest copy as the primary player-facing explanation; external comprehension remains pending. | `.game-harness/visual/same-order-visual-evidence-proxy-2026-05-16.md`, `.game-harness/visual/same-order-manual-readability-review-2026-05-17.md` |

## Runtime Evidence

Latest current-affordance addendum: world-record prop inspection now separates
possible role actions from actions currently open in the present object state.
In the inquest Codex run, `park_notice_board=rumored` shows current open
actions such as `공개 게시` and `수습 게시`; `studio_review_queue=blocked`
still shows its role/action map but also says `현재 열린 행동: 없음`. Codex
gameplay QA fails if the blocked review queue again looks currently actionable.

Latest NPC action-gate addendum: NPC reaction inspection now explains why an
NPC action was valid in the environment. Inspecting Studio PM shows `가능 조건`
for `스튜디오 리뷰 줄=열림`, the cited Station ledger, and `값 변화` for trust
and burden; inspecting Waiting Customer shows `대기 표식=줄 흐트러짐`, the same
cited ledger, and the `신뢰-8, 부담+5` effect behind contact refusal. Codex
gameplay QA fails if those player-readable action gates disappear.

Latest civic-economy inspection addendum: the civic economy panel is no longer
only a compact HUD label. Player/Codex can focus `civic_economy_panel` and
interact to read current tiny social values for `잔액`, `신뢰`, `부담`, and
`주목`, followed by recent ledger lines with `변화` deltas. Codex gameplay QA
now fails if the panel cannot explain which visible role action moved those
values from the player point of view.

Latest repair-conditional addendum: repair recovery now leaves a public
environment record and one small cross-place consequence. Park Witness
`post_repair_notice` changes `park_notice_board` to `repaired`; Studio PM then
reads that public repair notice and `studio_review_queue`, uses
`offer_conditional_review`, writes `studio_review_conditioned`, changes
`studio_review_queue` to `conditional`, and becomes visible with `조건부 리뷰`.
The live HUD consequence reads `기억 공백 발화 -> 정정표 -> 대기줄 수습 -> 공개
수습 게시 -> 조건부 리뷰`; Codex gameplay QA focuses and inspects both the Park
notice board and the conditional Studio review queue. The repair route now
fails if the public repair stays local-only or if the conditional review
consequence is hidden.

Latest soft-report HUD addendum: the soft-report route no longer hides the
local social chain behind a generic report-filed line. Its HUD consequence now
reads `플레이어 발화 -> 상점 기록 -> 공원 게시 -> 응대 중단 -> 줄 이탈 -> 보고
접수`, matching the existing Park Witness rumor, Store Manager service pause,
and Waiting Customer queue exit. Playable smoke and Codex gameplay QA fail if
those social steps disappear from the soft-report player-visible consequence.

Latest Store Manager addendum: the manager's intermediary action is now
player-readable as distinct NPC state. Soft report shows `NPC_Store_Manager`
as `paused` with `응대 중단` after `pause_service`; inquest shows the same actor
as `forwarded` with `보고 전달` after `forward_report`. Playable smoke and
Codex gameplay QA now fail if the manager's local service pause and formal
report handoff collapse back into a generic reported marker.

Latest public-rumor addendum: Park Witness `post_rumor` is now player-readable
as an NPC reaction in the soft-report and inquest paths. The actor shows
`rumored` / `소문 게시`, and the soft-report outcome chain says
`공원 소문 게시` before counter pause, queue exit, and Station warning intake.
Playable smoke and Codex gameplay QA now fail if this public rumor is only a
ledger/prop mutation.

Latest NPC reaction-inspection addendum: player/Codex inspection now labels the
record actor and validated action behind a visible NPC reaction. In the inquest
route, Studio PM inspection reads `civic-ledger-7 / 스튜디오 PM -> 리뷰 차단`
and the cited record reads `civic-ledger-6 / 스테이션 직원 -> 기록 인용`; Waiting
Customer inspection reads `civic-ledger-8 / 대기 손님 -> 접촉 거부` and the same
Station citation source. Codex gameplay QA fails if those actor/action labels
disappear from the HUD body or exported snapshot fields.

Latest live-HUD consequence addendum: the inquest HUD consequence line now
continues through the social result that already exists in the scene. Once the
Station citation blocks `studio_review_queue` and the Waiting Customer refuses
contact, the HUD reads the chain through `스튜디오 리뷰 차단 -> 접촉 거부`
instead of stopping at `스테이션 인용`. Codex gameplay QA and renderer capture
both fail if that player-readable chain regresses.

Latest warning-side Studio addendum: the suspicious-cover route now proves that
a public warning can close a tiny opportunity outside Store/Station. After
Park Witness posts `public_warning_posted`, Studio PM reads the Park notice
board and `studio_review_queue`, uses `defer_review`, writes
`studio_review_deferred`, changes `studio_review_queue` to `deferred`, and
shows `NPC_Studio_PM` as `deferred` with `리뷰 보류`. Codex route reports also
focus the Studio review queue and inspect the deferred HUD body, so this is a
player-readable cross-place consequence, not only a ledger mutation.

Latest NPC-inspection addendum: player/Codex can now inspect a visible NPC
through the same focus plus interact path used for readable world records. In
the inquest route, `focus.npc` on `NPC_Waiting_Customer` followed by
`player.interact.focused` opens a HUD notice with `현재 반응: 접촉 거부`, the
actor pressure line, `근거 행동: civic-ledger-8 / 대기 손님 -> 접촉 거부`, `읽은
기록: civic-ledger-6 / 스테이션 직원 -> 기록 인용`, and `대상 기록물: 대기 표식`.
The snapshot also exports `basisLedgerEventId`, `basisLedgerEventLabel`,
`citedLedgerEventId`, `citedLedgerEventLabel`, `basisAffordance`, and
`basisObjectLabel`. This makes an NPC's social stance and the record that
caused it a playable read target, not just a marker or JSON summary.

Latest Waiting Customer addendum: final social decisions are now readable on
the actor who makes them, not only in ledger state or route summaries. Clean
cover shows `NPC_Waiting_Customer` as `helped` with `도움`, repair recovery
shows `repair_accepted` with `수습 수락`, suspicious cover shows `distanced`
with `거리두기`, soft report shows `left` with `줄 이탈`, and inquest shows
`refused` with `접촉 거부`. Codex route reports, playable smoke, and packaged
route smoke preserve those markers; the packaged app inquest proof includes
`NPC_Waiting_Customer` state `refused` with reaction text `접촉 거부`.

Latest public-notice addendum: the Park Witness public-record actions are now
visible as NPC state, not only ledger and prop changes. Clean cover shows
`NPC_Park_Witness` as `vouched` with `공개 확인`, repair recovery shows the same
actor as `repaired` with `수습 게시`, and suspicious cover shows the actor as
`warned` with `공개 경고`. Codex route reports and playable smoke now require
those player-readable markers before the route proof can pass.

Latest clean-route addendum: the clean route now also proves
`studio_review_queue=invited`, `studio_review_invited`, local trust 60, and
Studio PM `invite_review` from the Park public routine vouch. This is a tiny
cross-place opportunity loop, not a Studio-system expansion. Codex/player also
opens that invited review queue through `focus.world_record_prop` plus
`player.interact.focused`, proving the invitation is readable from the running
HUD and not only from route summary data. The same clean route now also marks
the spawned `NPC_Studio_PM` as `invited` with a visible review-open reaction,
so the actor who used the public record is readable in the scene.

Shell, runtime, and playable Evidence Packs validate through backend `validateGodotEvidencePack`. The playable pack now includes `conversation_started`, `dialogue_choice_selected`, `response_hesitation_noted`, `free_input_submitted` with `freeInputHash`, `conversation_anomaly_detected`, `npc_suspicion_changed`, `suspicion_shared`, `station_report_created`, and `station_inquest_opened` for the hard route, plus route proofs for clean cover, repair recovery, soft report, and inquest. The current hard-route playable summary now proves `station_record_cited`, `studio_review_blocked`, `queue_contact_refused`, `studio_review_queue=blocked`, `store_queue_mark=refused`, `recordBurden=93`, Station attention 70, Station Officer `cite_record`, Studio PM `block_review`, and Waiting Customer `refuse_contact` after the Station citation. The clean-cover/suspicious-cover/repair/soft-report/inquest agentic and playable summaries carry `socialObservationTrace` showing which prior NPC record and civic economy pressure caused the next role action. The current playable pack also carries `playability.providerActionComparison`, `playability.providerSchedulingPlan`, `playability.providerDispatchContract`, `playability.storyletRuntimeMap`, `playability.comprehensionProxy`, `playability.playerComprehensionPlaytestPacket`, `playability.visualEvidenceProxy`, and `playability.assetBillOfMaterials` generated from backend harnesses. Three-run Evidence Pack diversity validates through `validateGodotEvidencePackTrajectoryDiversity`.

## Playtest Evidence

Pending external player comprehension test. Current playability evidence includes fresh Godot smoke/capture, NPC-to-NPC social observation proof for soft report and inquest, all-route terminal outcome role-action proof, Same Order comprehension proxy, player comprehension packet, pending external comprehension notes ledger, packaged-app comprehension session helper, raw-note review helper, visual evidence proxy, renderer-backed screenshots/contact sheet, and manual readability review of the current capture. Blind player notes remain required.

| Artifact | Result | Output Summary |
|---|---|---|
| `.game-harness/scripts/run-same-order-comprehension-session.sh --recruitment` | pass | Re-runs packaged preflight without printing preflight details, self-checks the invite with the broad no-spoiler scan, then prints only the tester-safe invite. It avoids revealing records, reports, risk, Station citation, provider mode, app paths, evidence paths, or intended player role before first play and does not count as player comprehension. | `.game-harness/scripts/run-same-order-comprehension-session.sh` |
| `.game-harness/scripts/run-same-order-comprehension-session.sh --facilitator-pack` | pass | Re-runs packaged preflight, then prints the facilitator-only run pack with current app/evidence hashes, no-spoiler invite text, before-play instruction, after-play debrief prompts, worksheet path, and review commands. It must not be sent to testers or counted as player comprehension. | `.game-harness/scripts/run-same-order-comprehension-session.sh` |
| `.game-harness/scripts/run-same-order-comprehension-session.sh --facilitator-pack-output <path>` | pass | Re-runs packaged preflight, writes the same facilitator-only run pack to the requested path, and reminds the facilitator that the file is not tester-facing and not comprehension evidence. | `.game-harness/scripts/run-same-order-comprehension-session.sh` |
| `.game-harness/scripts/run-same-order-comprehension-session.sh --session-kit-output <dir>` | pass | Re-runs packaged preflight and writes a session kit directory containing run pack, tester-safe invite, facilitator-only recruitment companion, facilitator card, after-play debrief, blank worksheet, gate status, and README. Only `tester-invite.md` is tester-facing; the kit cannot close the player-comprehension gate. | `.game-harness/scripts/run-same-order-comprehension-session.sh` |
| `.game-harness/scripts/run-same-order-comprehension-session.sh --instructions` | pass | Re-runs packaged preflight, prints the exact facilitator prompt, current packaged app/evidence paths, notes directory, and review commands without launching the app. This prepares the external test but does not count as player comprehension. | `.game-harness/scripts/run-same-order-comprehension-session.sh` |
| `.game-harness/scripts/run-same-order-comprehension-session.sh --debrief` | pass | Re-runs packaged preflight, then prints the after-play C1-C7-style question card and direct quote prompts without launching the app. This reduces missed post-play questions but does not count as player comprehension. | `.game-harness/scripts/run-same-order-comprehension-session.sh` |
| `.game-harness/scripts/run-same-order-comprehension-session.sh --worksheet` | pass | Re-runs packaged preflight, then prints a blank raw-note worksheet with the exact strict-review fields plus current packaged app and route evidence hashes. This supports remote or paper-note sessions but does not count as player comprehension until filled from observed fresh-player play. | `.game-harness/scripts/run-same-order-comprehension-session.sh` |
| `.game-harness/comprehension/manual-sessions/README.md` | setup | Provides a short facilitator-facing quality guide for raw tester notes, emphasizing direct quotes, observed play only, and the dialogue-to-record-to-consequence-to-role-action explanation. This does not count as player comprehension. | `.game-harness/comprehension/manual-sessions/README.md` |
| `.game-harness/scripts/run-same-order-comprehension-session.sh --status` | pass | Re-runs packaged preflight but suppresses the successful preflight detail at `0 / 3` notes, reports strict review status `PENDING_TESTER_NOTES`, prints the live fresh-player session as the first next action, summarizes Codex QA setup readiness, and avoids dumping the full raw-note review table until raw notes exist. This is a status helper only; file count alone cannot close the gate. | `.game-harness/scripts/run-same-order-comprehension-session.sh` |
| `.game-harness/scripts/review-same-order-comprehension-notes.sh` | pending | Reports `PENDING_TESTER_NOTES` with `Manual session count: 0`; no raw tester notes exist yet. | `.game-harness/comprehension/same-order-external-comprehension-notes-2026-05-17.md` |
| `.game-harness/scripts/review-same-order-comprehension-notes.sh --strict` | expected fail | Exits non-zero while raw tester notes are `0 / 3`; this confirms generated packets, screenshots, and proxy checks cannot close the player-comprehension gate. | `.game-harness/scripts/review-same-order-comprehension-notes.sh` |
| `.game-harness/scripts/verify-comprehension-review-guards.sh` | pass | Verifies tester-facing invite output stays no-spoiler, proves standalone `--recruitment` fails before printing when a broad/case-insensitive leak such as `Record` appears, proves session-kit verification fails if manifest freshness is stale, if the kit's copied Codex artifacts are older than current proof-cell files, if `dialogue.choice.by_index` is missing from the Codex action catalog binding, if the copied Codex probe loses the accepted typed free-input step, player-visible HUD record state, Station Officer citation state, readable Markdown gameplay report content, or README live-session handoff, then creates temporary placeholder notes with valid-looking app/evidence hashes, runs strict review against them, and confirms strict review fails with direct quote counts at `0 / 3`. This protects the gate from leaked setup spoilers, stale AI-play setup, AI-play action drift, unreadable gameplay reports, and checkbox/filler notes. | `.game-harness/scripts/verify-comprehension-review-guards.sh` |

Latest external-comprehension blocker snapshot:

- Packaged preflight is tester-ready.
- Current local Godot visual proof has 8 captures and no blocked checks.
- Backend checks pass after attaching playability reports.
- Raw external session notes are still `0 / 3`, so the active goal remains open.
