# ADR-0001: 리포지터리 레이아웃 및 문서 분리

- 상태: Accepted
- 배경: Unity 클라이언트와 향후 백엔드를 분리하고, 문서를 찾기 쉽게 구성
- 결정: Unity 프로젝트는 `game/`에, 백엔드는 `server/`에(향후), 루트 문서는 `docs/`에, 게임 문서는 `game/docs/`에 배치
- 결과: 명확한 경계, 간단한 CI 타겟팅; 기존 프로젝트 이동 시 경로 업데이트 필요
